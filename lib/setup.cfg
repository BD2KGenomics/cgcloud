[pytest]
# Look for any python file, the default of test_*.py wouldn't work for us
python_files=*.py
# Run doctests and start test collection in the src dir
addopts = --doctest-modules -k "not cgcloud_Crypto"

[nosetests]
with-doctest=1
exclude=^cgcloud_Crypto\.
